---
interface Props {
	title: string;
	body: string;
}

const { title, body } = Astro.props;
---
<div class="px-3 py-1 leading-6 text-gray-600 my-8">
	<div class="font-semibold text-xl leading-7 text-gray-900">
	{title}
  </div>
  <p class="mt-2 text-base leading-7 text-gray-600">{body}</p>
</div>